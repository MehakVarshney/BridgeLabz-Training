class FlightInfo {
    int passengerCapacity;
    int fuelCapacity;   // in liters (example)

    FlightInfo(int passengerCapacity, int fuelCapacity) {
        this.passengerCapacity = passengerCapacity;
        this.fuelCapacity = fuelCapacity;
    }
}
